---
description: Learn about flexbox layout and its configuration
---

import Tabs from "@theme/Tabs";
import TabItem from "@theme/TabItem";

# Flexbox 📦

Flexbox is a one-dimensional layout system that we can use to create a row or a column axis layout. It makes our life easier to design and build responsive web pages without having to use tricky hacks and a lot of float and position properties in our CSS code.

## Types

- `flex`: `flex` property
- `flex-wrap`: `flex-wrap` property
- `flex-shrink`: `flex-shrink` property
- `flex-grow`: `flex-grow` property
- `flex-basis`: `flex-basis` property
- `gap`: `gap` property
- `order`: `order` property
- `ai`: `align-items` property
- `ac`: `align-content` property
- `jc`: `justify-content` property


## Usage

This is a simple flexbox usage with tenoxui :

```html
<div class="d-flex gap-1rem jc-center">
  <div class="flex-[1\_1\_200px]">...</div>
  <div class="flex-[1\_1\_200px]">...</div>
  <div class="flex-[1\_1\_200px]">...</div>
</div>
```