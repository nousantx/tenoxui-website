---
title: All Classes 🏫
---

import Tabs from "@theme/Tabs";
import TabItem from "@theme/TabItem";


# All Classes

Explore All Class and Property that you can use or already defined by TenoxUI.

## Align 🎯
- `ai` : `align-items`
- `ac` : `align-content`
- `as` : `align-self`

## Aspect Ratio 🌌

- `ratio` : `aspect-ratio`

## Backdrop Filter 🏖️

- `back-blur` : `backdrop-filter: blur();`
- `back-brightness` : `backdrop-filter: brightness();`
- `back-contrast` : `backdrop-filter: contrast();`
- `back-grayscale` : `backdrop-filter: grayscale();`
- `back-saturate` : `backdrop-filter: saturate();`
- `back-sepia` : `backdrop-filter: sepia();`

## Background 🖼️

- `bg` : `background`
- `bg-size` : `background-size`
- `bg-clip` : `background-clip`
- `bg-repeat` : `background-repeat`
- `bg-loc` : `background-position`
- `bg-loc-x` : `background-position-x`
- `bg-loc-y` : `background-position-y`
- `bg-blend` : `background-blend`

## Border 🛡️

- `bdr` : `border`
- `br` : `border-radius`
- `bw` : `border-width`
- `bs` : `border-style`
- `bc` : `border-color`
- `border` : `border-color`. On `moreColor` only.
- `bw-top` : `border-top-width`
- `bw-bottom` : `border-bottom-width`
- `bw-left` : `border-left-width`
- `bw-right` : `border-right-width`
- `radius-tl` : `border-top-left-radius`
- `radius-bl` : `border-bottom-left-radius`
- `radius-tr` : `border-top-right-radius`
- `radius-br` : `border-bottom-right-radius`
- `radius-top` : [ `br-tl`, `br-tr` ],
- `radius-bottom` : [ `br-bl`, `br-br` ],
- `radius-left` : [ `br-tl`, `br-bl` ],
- `radius-right` : [ `br-tr`, `br-br` ],
- `br-ss` : `border-start-start-radius`
- `br-se` : `border-start-end-radius`
- `br-ee` : `border-end-end-radius`
- `br-es` : `border-end-start-radius`
- `bw-is` : `border-inline-start-width`
- `bw-ie` : `border-inline-end-width`

## box-sizing 🖥️

- `box-sizing` : `box-sizing`

## Cursor 🖱️

- `curs`, `cursor` : `cursor`

## Display 🚀

- `display`, `dis` : `display`

## Filter 📷

- `blur` : `filter: blur();`
- `brightness` : `filter: brightness();`
- `contrast` : `filter: contrast();`
- `grayscale` : `filter: grayscale();`
- `hue-rotate` : `filter: hue-rotate();`
- `saturate` : `filter: saturate();`
- `sepia` : `filter: sepia();`

## Flexbox 📦

- `fx`, `flex` : `flex`
- `auto-flex`, `flex-auto`, `fx-auto` : `flex: 1 1 {value};`
- `initial-flex` : `flex: 0 1 {value}`
- `fx-wrap` : `flex-wrap`
- `fx-grow` : `flex-grow`
- `fx-shrink` : `flex-shrink`
- `fx-basis` : `flex-basis`
- `gap` : `gap`
- `item-order`, `order` : `order`
- `ai` : `align-items`
- `jc` : `justify-content`
- `flex-parent`, `fx-par` : [`justify-content`, `align-items`]

## Float ☁️

- `float` : `float`

## Font & Text ⌨️

- `fs` : `fontSize`
- `fw` : `fontWeight`
- `lh` : `lineHeight`
- `ls` : `letterSpacing`
- `ta` : `text-align`
- `tc` : `color`
- `ts` : `textStyle`
- `td` : `textDecoration`
- `ti` : `textIndent`
- `tn` : `textReansform`
- `ws` : `wordSpacing`
- `family` : `fontFamily`
- `text-style` : `fontStyle`
- `white-space` : `whiteSpace`
- `text-over` : `textOverflow`
- `text-wrap` : `textWrap`
- `v-align` : `verticalAlign`
- `w-break` : `wordBreak`
- `wrap-over` : `overflowWrap`
- `hyphens` : `hyphens`

## Height 📏

- `h` : `height`
- `h-mn` : `min-height`
- `h-mx` : `max-height`

## Isolation 📎

- `isolation`, `isolate` : `isolation`

## Justify 🌌

- `ji` : `justify-items`
- `jc` : `justify-content`
- `js` : `justify-self`

## List Style 🗒️

- `list-s` : `list-style`
- `list-s-img` : `list-style-image`
- `list-s-pos` : `list-style-position`
- `list-s-type` : `list-style-type`

## Margin ↔️

- `m` : `margin`
- `mt` : `margin-top`
- `mb` : `margin-bottom`
- `ml` : `margin-left`
- `mh` : `margin-right`
- `mr` : `margin-right`
- `mv` : [ `margin-top`, `margin-bottom` ]
- `mh` : [ `margin-left`, `margin-right` ]

## Padding ↔️

- `p` : `padding`
- `pt` : `padding-top`
- `pb` : `padding-bottom`
- `pl` : `padding-left`
- `ph` : `padding-right`
- `pr` : `padding-right`
- `pv` : [ `padding-top`, `padding-bottom` ]
- `ph` : [ `padding-left`, `padding-right` ]

## Opacity & Visibility 🔍

- `opa` : `opacity`
- `visibility` : `visibility`

## Outline ☑️

- `ol` : `outline`
- `ol-offset` : `outline-offset`
- `ol-style` : `outline-style`
- `ol-width` : `outline-width`

## Overflow ⬆️
Overflow property :

- `over` : `overflow`
- `overX` : `overflowX`
- `overY` : `overflowY`

## Position 📍

- `post`, `position` : `position`
- `t`, `top` : `top`
- `b`, `bottom` : `bottom`
- `l`, `left` : `left`
- `r`, `right` : `right`
- `z`, `zi` : `z-index`

## Transform 🔁

- `transform` : `transform`
- `translate` : `transform: translate()`
- `matrix` : `transform: matrix()`
- `matrix-3d` : `transform: matrix3d()`
- `move-x` : `transform: translateX()`
- `move-y` : `transform: translateY()`
- `move-z` : `transform: translateZ()`
- `rt` : `transform: rotate()`
- `rt-3d` : `transform: rotate3d()`
- `scale` : `transform: scale()`
- `scale-x` : `transform: scaleX()`
- `scale-y` : `transform: scaleY()`
- `scale-z` : `transform: scalez()`
- `skew` : `transform: skew()`
- `skew-x` : `transform: skewX()`
- `skew-y` : `transform: skewY()`

## Transition 🔄

- `tr` : `transition`
- `tr-delay` : `transition-delay`
- `tr-prop` : `transition-property`
- `tr-time` : `transition-duration`
- `tr-timing` : `transition-timing-function`

## Width 📐

- `w` : `width`
- `w-mn` : `min-width`
- `w-mx` : `max-width`

## Custom Type and Property ✨

You can make your own type and property by using [addType and defineProps](/docs/function/addtype-and-defineprops) function. Here's simple usage :

```html title="html"
<div class="my-box-type-300px"></div>
<div class="my-bg-type-blue"></div>
```
```js title="javascript"
defineProps({
  boxs: ["-webkit-box-sizing"],
  "my-box-type": ["width", "height"],
  "my-bg-type": ["background-color"]
})
```

## Closing 🚪

See current available type and property on [GitHub Repository](https://github.com/nousantx/tenoxui/blob/main/src/js/lib/property.js).
